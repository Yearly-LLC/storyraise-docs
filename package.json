{
  "name": "storyraise-docs",
  "private": true,
  "description": "Storyraise Knowledge Base — markdown sources in content/, built to static HTML in docs/ by build/build.js",
  "scripts": {
    "build": "node build/build.js"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
